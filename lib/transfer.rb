class Transfer
  # space for a transaction between two instances of bank acct class
  #checks validity of account before transaction occurs
  #can reject a transfer if accts arent valid or if no money
  #start in "pending", end as "complete" or "rejected"
  #complete transfer can be reversed, go into "reversed" status
end
